apiVersion: apps/v1
kind: Deployment
metadata:
  name: solid-lendig-platform
  namespace: fintech-prod-kale-team
  labels:
    app: solid-lendig-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: solid-lendig-platform
  template:
    metadata:
      annotations:
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "9090"
        prometheus.io/scrape: "true"
      labels:
        app: solid-lendig-platform
        deploysphere_health: "true"
    spec:
      imagePullSecrets:
        - name: fintech-docker-secret
      containers:
        - name: solid-lendig-platform
          image: kale-team-docker-registry.fintechchallenge.pl/kale-team/solid-lendig-platform:latest
          imagePullPolicy: Always
          ports:
            - name: api
              containerPort: 8080
            - name: management
              containerPort: 9090
          resources:
            limits:
              cpu: 400m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 32Mi
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 120
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 50
            periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: solid-lendig-platform
  namespace: fintech-prod-kale-team
  labels:
    app: solid-lendig-platform
    visualize: "true"
    prometheus_monitoring: "true"
spec:
  ports:
    - name: api
      port: 8080
      protocol: TCP
      targetPort: api
  selector:
    app: solid-lendig-platform
---









---
# Note: in a kubernetes secret the string (e.g. generated by htpasswd) must be base64-encoded first.
# To create an encoded user:password pair, the following command can be used:
# htpasswd -nb user password | openssl base64

apiVersion: v1
kind: Secret
metadata:
  name: fintech-prod-kale-team-basic-auth-secret
  namespace: fintech-prod-kale-team

data:
  users: |
    ZXhhbXBsZTokYXByMSROOG1OUFFKTiRJWTVyZnBTenJEZkszTmN1bkhTbWQvCgo=
#encryped username: example, with password: example

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: fintech-prod-kale-team-basic-auth-middleware
  namespace: fintech-prod-kale-team
spec:
  basicAuth:
    secret: fintech-prod-kale-team-basic-auth-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: solid-lendig-platform
  namespace: fintech-prod-kale-team
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`solid-lendig-platform.kale-team.fintechchallenge.pl`)
      middlewares:
        - name: fintech-prod-kale-team-basic-auth-middleware
          namespace: fintech-prod-kale-team
      services:
        - kind: Service
          name: solid-lendig-platform
          namespace: fintech-prod-kale-team
          passHostHeader: true
          port: 8080
          scheme: http
  tls:
    options:
      namespace: fintech-prod-kale-team
    certResolver: hltech
    domains:
      - main: solid-lendig-platform.kale-team.fintechchallenge.pl
